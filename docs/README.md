# All Url Uploader Docs

This folder contains the external documentation site for the project.

The bot runtime does not depend on `docs/`, so you can work on the Telegram bot from the repository root without touching the documentation app.
